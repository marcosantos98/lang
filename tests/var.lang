import "std:c"

thing :: void(thing bool) {
    exit(thing)
}

main :: void() {
    var a = 10
    var b = "Hello"
    var c = true
    var d = false
    
    puts(to_cstring(b))

    thing(d)
}
