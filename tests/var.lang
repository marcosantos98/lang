import "std:c"

thing :: void(thing bool) {
    exit(thing)
}

main :: void() {
    var a = 10
    var b = "Hello"
    var c = true
    var d = false
    
    printf(c("%s"), b.data)

    thing(d)
}
