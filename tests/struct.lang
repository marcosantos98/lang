import "std:c"

struct Bar {
    baz int,
}

struct Foo {
    bar int,
    baz int,
}

main :: void() {
   var foo = Foo{10, 20}

   var b: int = foo.baz
	
   printf(xx to_cstring("%d\n"), foo.bar)
   printf(xx to_cstring("%d\n"), foo.baz)
}
